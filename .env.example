# Netlify 배포용 환경 변수 예시
# Netlify 대시보드의 Site settings > Environment variables에서 설정

# API 서버 URL (백엔드 서버 주소)
VITE_API_URL=https://your-backend-server.herokuapp.com

# 또는 로컬 개발용
# VITE_API_URL=http://localhost:4001

# 데이터베이스 연결 (서버 측에서만 사용)
DATABASE_URL=your_database_connection_string

# 세션 시크릿 (서버 측에서만 사용)
SESSION_SECRET=your_session_secret_key

# 프로덕션 플래그
NODE_ENV=production
